export const metadata = {
  title: 'Send Email | Admin | Labor Party',
  description: 'Send emails to chapter members via MailerLite.',
}

export default function EmailLayout({ children }) {
  return children
}
